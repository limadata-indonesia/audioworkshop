import ContentPage from "@/components/ContentPage";

export const metadata = { title: "Quality Policy — Audio Workshop Indonesia" };

export default function Page() {
  return <ContentPage slug="quality-policy" />;
}
